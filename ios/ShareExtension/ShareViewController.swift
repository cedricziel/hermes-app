import receive_sharing_intent

/// Saves the shared content into the App Group container and opens Hermes,
/// where the chat composer picks it up.
class ShareViewController: RSIShareViewController {}

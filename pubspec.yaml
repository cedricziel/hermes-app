name: hermes_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 0.1.34 # x-release-please-version

environment:
  sdk: ^3.13.4

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.9

  # HTTP client for the Hermes dashboard REST API.
  dio: ^5.11.1
  # Generated dio-based client for the Hermes Agent REST API (see
  # openapi/hermes-agent.openapi.json and scripts/generate_hermes_api_client.sh).
  hermes_api:
    path: packages/hermes_api
  # OS keychain / keystore for the native OAuth token set.
  flutter_secure_storage: ^11.2.0
  # Opens the system browser for the RFC 8252 native login flow.
  url_launcher: ^6.3.2
  # Persists non-secret app settings (server URL) across launches.
  shared_preferences: ^2.5.5
  # App-wide state management (auth session, connection settings).
  provider: ^6.1.5+1
  # PKCE code_challenge (SHA-256) for the native OAuth flow.
  crypto: ^3.0.7
  receive_sharing_intent: ^1.9.0
  flutter_chat_ui: ^2.12.0
  flutter_chat_core: ^2.9.1
  gpt_markdown: ^1.3.0
  flyer_chat_text_message: ^2.6.1
  material_ui: ^1.4.0
  # OpenTelemetry logs and traces, exported to SignalDB.
  flutter_otel: ^0.1.1
  # Coarse device facts for the telemetry resource.
  flutter_otel_device_info: ^0.1.0
  # Request spans and logs that record the route only, never the server address.
  dart_otel_instrumentation_dio: ^0.1.1
  # The chat WebSocket as an upgrade span plus linked message spans.
  dart_otel_instrumentation_messaging: ^0.1.1
  # The dashboard's /api/ws JSON-RPC socket the chat streams over.
  web_socket_channel: ^3.0.3
  stream_channel: ^2.1.4
  flutter_local_notifications: ^22.3.1
  characters: ^1.4.1
  upgrader: ^13.7.0
  version: ^3.0.2
  local_auth: ^3.0.2
  file_picker: ^13.1.0
  image_picker: ^1.2.3
  desktop_drop: ^0.8.4
  pasteboard: ^0.5.0
  # Opens a downloaded file in the system's app for its type, on all five
  # platforms (open_filex is mobile only).
  open_file: ^4.0.0
  # The app's cache directory, where media the agent sent is downloaded.
  path_provider: ^2.1.6
  flutter_json_view: ^1.1.5
  connectivity_plus: ^7.3.1
  # App version shown in the About dialog.
  package_info_plus: ^10.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Drives the real app for the store screenshots (integration_test/).
  integration_test:
    sdk: flutter
  # In-memory SharedPreferencesAsyncPlatform for widget tests.
  shared_preferences_platform_interface: ^2.4.2

  # Component catalog (widgetbook/), run with -t widgetbook/main.dart.
  widgetbook: ^3.25.0

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  http: ^1.6.0
  fake_async: ^1.3.3
  # Staged-file format and analyze hook, installed by tool/setup_git_hooks.dart.
  dart_pre_commit: ^6.1.5

dart_pre_commit:
  outdated:
    level: major
    allowed:
      - code_assets
      - record_use
  pull-up-dependencies: true
  osv-scanner: false

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
